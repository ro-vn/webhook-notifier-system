server:
  port: 8081

spring:
  threads:
    virtual:
      enabled: true

logging:
  level:
    com.rnvo.receiver: INFO

management:
  endpoints:
    web:
      exposure:
        include: prometheus
